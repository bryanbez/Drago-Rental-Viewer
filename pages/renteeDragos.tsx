import { DragosPerRenteeAddress } from 'components/DragosPerRenteeAddress';
import { ScrollView } from 'react-native';
export const RenteeDragosPage: React.FC = () => {
  return (
    <ScrollView>
      <DragosPerRenteeAddress />;
    </ScrollView>
  );
};
