import { DisplayCard } from 'components/DisplayCard';
import { View } from 'react-native';

export const DragosPage: React.FC = () => {
  return (
    <View className="flex-1">
      <DisplayCard />
    </View>
  );
};

// dont remove the flex-1
